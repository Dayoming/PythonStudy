# 2차원 리스트를 만들고 요소에 접근하기
a = [[10, 20],
     [30, 40],
     [50, 60]]
print(a)

a = [[10, 20],
     [30, 40],
     [50, 60]]
print(a[1][1])

# 2차원 리스트를 만들 때 리스트 안에 튜플을 넣거나, 튜플 안에
# 리스트를 넣는 방식도 사용할 수 있다.

# 리스트 = [(값, 값), (값, 값), (값, 값)]
# 튜플 = ([값, 값], [값, 값], [값, 값])
# 튜플 = ((값, 값), (값, 값), (값, 값))