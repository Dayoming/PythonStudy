print(4.3 - 2.7)
# 컴퓨터는 실수를 표현할 때 오차가 발생한다.

print(4.2 + 5)
# 실수와 정수를 함께 계산하면 표현 범위가 넓은 실수로 계산된다.

print(float(1 + 2))
# 계산 결과가 정수이지만 강제로 실수로 만들고 싶을 때

print(1.2 + 1.3j)
# 파이썬에서는 실수부와 허수부로 이루어진 복소수(complex number)도 사용할 수 있다.
# 이 때 허수부는 숫자 뒤에 j를 붙인다. (수학에서는 허수를 i로 표현하지만 공학에서는 j로 표현)